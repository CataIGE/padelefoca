services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: padelefoca-db
    environment:
      MSSQL_SA_PASSWORD: "Admin2024!"
      ACCEPT_EULA: "Y"
    ports:
      - "1434:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql

volumes:
  sqlserver_data: